# 통신 규약 (PROTOCOL)

직접 다른 발송 프로그램을 만들거나, 다른 수신 단말을 붙이려면 이 문서만 보면 됩니다.
모든 통신은 평문 UTF-8 이며 별도 서버가 없습니다.

## 1. 채널 이름

```
base            임의의 문자열. 모든 교실 공통.   예) myschool-call-3f7a91c2
반별 채널        base-<학년>-<반>                예) myschool-call-3f7a91c2-2-8   (2학년 8반)
회신 채널        base-reply                      교실 → 교무실 (수신확인·답장)
```

- 보내는 쪽은 **반별 채널로만** 보냅니다. (전 반 동시 방송 기능은 없습니다 — 필요한 반을 각각 고릅니다.)
- `base` 에 반을 붙여 적는 실수(`...-2-8`)가 잦아, 받는 쪽은 끝의 `-숫자-숫자`를 자동으로 떼어냅니다.
- 채널 이름은 사실상 비밀번호입니다. 무작위 문자열을 쓰세요.

## 2. 호출 보내기

### 2-1. 인터넷 중계 (ntfy.sh)

```http
POST https://ntfy.sh/<base>-<학년>-<반>
Priority: urgent
Tags: bell,cid-a3f09c

지수야~ 교무실로 오세요
— 권혁규T 로부터
```

- 본문 **첫 줄**이 호출 문구(크게 표시), **나머지 줄**은 보낸 사람(작게 표시).
- `Tags` 의 `cid-xxxxxx` = 호출 번호(6자리 16진수). 받는 쪽이 이 번호를 그대로 돌려줍니다.
- 보낸 시각은 ntfy 가 메시지에 붙여주는 `time`(유닉스 초)을 씁니다.
- 받는 쪽은 `https://ntfy.sh/<채널>/json` 스트림을 구독합니다. 끊기면 `?since=<마지막 id>` 로 다시 붙어
  그사이 호출을 놓치지 않되, **180초보다 오래된 호출은 버립니다**(엉뚱한 때 뜨는 것 방지).

### 2-2. 학교망 직접 (전자칠판 앱이 여는 TCP 포트, 기본 8787)

```http
POST http://<칠판IP>:8787/
X-Call-Id: a3f09c
X-Sent: 1789959248
X-Key: <선택. 앱에 암호를 넣었다면 같은 값>
Content-Type: text/plain; charset=utf-8

지수야~ 교무실로 오세요
— 권혁규T 로부터
```

- 응답 `200 {"ok":true,...}` 자체가 **수신 확인**입니다(앱이 화면에 띄우기로 했다는 뜻).
- `GET /` 은 상태 확인용이며 같은 JSON을 돌려줍니다:
  `{"ok":true,"app":"classcall","class":"2-8","label":"2학년 8반","room":"","ip":"10.1.2.3","port":8787}`

### 2-3. 칠판 자동 찾기 (UDP 8787)

```
→ UDP 브로드캐스트  "CLASSCALL?"
← 응답             "CLASSCALL!{"ok":true,"class":"2-8","ip":"10.1.2.3","port":8787,...}"
```

같은 대역에 있는 칠판들이 자기 반과 주소를 알려줍니다. 보내는 쪽은 이걸로 반↔IP 표를 만듭니다.

## 3. 회신 (교실 → 교무실)

`POST https://ntfy.sh/<base>-reply` 로 JSON 한 줄을 보냅니다.

```jsonc
// 화면에 떴음 (자동, 표시 즉시)
{"t":"ack",   "cid":"a3f09c", "cls":"2-8", "via":"app"}

// 교실에서 답장 버튼을 누름
{"t":"reply", "cid":"a3f09c", "cls":"2-8", "via":"app", "reply":"네, 바로 보낼게요"}
```

- `via` 는 어디서 보냈는지 표시용: `app`(전자칠판 앱) / `html`(표시기) / `pc`(교실 PC 수신기).
- 보내는 쪽은 이 채널을 구독하고 있다가 `cid` + `cls` 로 어느 호출인지 맞춥니다.

## 4. 보내는 쪽 권장 동작

1. 호출마다 `cid` 를 새로 만든다.
2. 그 반 칠판 IP를 알고 있으면 **학교망 직접**을 먼저 시도한다.
3. 직접이 실패하면 **중계로 한 번만** 보낸다. (둘 다 보내면 칠판에 두 번 뜬다.)
4. 직접 성공 = 즉시 수신 확인. 중계로 보냈으면 `base-reply` 에서 `ack` 를 기다린다.
5. **15초 안에 `ack` 가 없으면 "송신 실패"** 로 표시한다(칠판이 꺼져 있거나 앱이 꺼진 상태).
6. 나중에 늦은 `ack` 가 오면 "늦게 수신 확인"으로 갱신한다.
7. `reply` 가 오면 어느 반이 무엇을 눌렀는지 함께 표시한다.

## 5. 표시 쪽 권장 동작

- 첫 줄은 글자 수에 따라 크기를 키워 화면에 꽉 차게, 나머지 줄은 작게(보낸 사람).
- 화면이 꺼져 있으면 깨우고(WakeLock), 수업 화면 위로 덮어쓴다(오버레이).
- 오버레이 권한을 줄 수 없는 기기라면 전체화면 알림(full-screen intent)으로 대체한다.
- 정해둔 시간(기본 60초) 뒤 자동으로 사라지고, 화면을 누르면 즉시 닫는다.
- 표시하는 즉시 `ack` 를 보낸다. 답장 버튼을 누르면 `reply` 를 보내고 닫는다.
